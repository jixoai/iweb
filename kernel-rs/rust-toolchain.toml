# rust-kernel-rustfs-storage §3.1：钉住工具链。
# 注意：rustup 管理的环境（CI、其它开发机）强制此版本；Homebrew cargo 会忽略本文件
# （1.90.0 与下方 pin 一致，行为等价）。
# 交叉编译说明：本仓库在 1.88（rustup）与 1.90（Homebrew）均通过完整测试套件；
# 钉 1.88 以匹配 rustup 侧可用的 linux 交叉 std。
[toolchain]
channel = "1.88.0"
profile = "minimal"
