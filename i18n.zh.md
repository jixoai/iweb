<!-- 用户原始需求（2026-08-13）：淬炼 iweb 一手架构资料中的领域共识词汇。 -->
<!-- 正交意图：固定中英术语；明确容易混淆的职责边界；为后续 Spec 与 UI 文案提供统一词源。 -->

# iweb 领域词汇

| 中文共识词 | 稳定英文 | 边界定义 |
| --- | --- | --- |
| 节点所有者 | node owner | 不要求具备容器、数据库或网络运维知识，通过 AI 编程代理管理个人节点的普通用户。 |
| 个人节点 | personal node | 一个所有者的持久控制边界；内部包含可信节点控制面与多个彼此隔离、不互信的应用。 |
| 应用包 | application package | 等待执行的代码与资产，可能来自网络复制或 AI 生成，无论提交者是谁均默认不可信。 |
| 应用沙箱 | application sandbox | 单个应用独占的执行、凭据、存储、网络、资源与生命周期边界；不是路由、handler 或 V8 isolate 的别名。 |
| 沙箱身份 | sandbox identity / `sandboxId` | supervisor 管理的运行时资源身份；由应用 ID、不可变版本 digest 与 sequence 确定性推导，不等同于版本身份。 |
| 版本身份 | version identity / `versionId` | Kernel 准入后形成的不可变应用版本身份；metrics 请求与响应必须显式逐值匹配。 |
| 沙箱监管器 | sandbox supervisor | 以宿主专用非特权身份运行、仅通过私有 Unix socket 接受有界生命周期指令的受信任服务；不是 Docker/Podman daemon API。 |
| 私有应用入口 | private application ingress | Kernel 到某个 ready sandbox 的唯一非公开流量入口；不反向授予应用访问 Kernel、宿主 loopback、控制面网络或其它沙箱的能力。 |
| 强制网络边界 | enforced network boundary | 在应用进程之外阻断 Worker 直连宿主、控制面、MinIO、公网和其它沙箱，仅允许经受信 Gateway 的明确路径；代理环境变量与服务端鉴权都不能替代它。 |
| 不可证明指标 | unavailable measurement | 无法证明 cgroup 归属、采样值或限制时的正式状态；必须贯穿 supervisor、Kernel、monitor 与 Admin，不能用 `0` 或节点总量替代。 |
| 受管沙箱身份 | managed sandbox identity | 生命周期操作前由可信记录、确定性 sandboxId 与运行时不可变标签共同确认的资源身份；仅有合法名称或可伪造标签不足以授权操作。 |
| 发布安全门 | publication gate | 显式启用与镜像内独立安全验收记录必须同时成立的失败关闭条件；任一缺失时禁止任意应用执行。 |
| 贡献者证据 | contributor evidence | 不要求贡献者拥有 Linux：以生产调用点、强类型契约、真实本地 socket、组装拓扑和可执行验收工具证明本地实现，但不得把 PLAN/mock 冒充宿主结果。 |
| 运营者验收 | operator acceptance | 由节点运营者、CI 或发布维护者在目标 Linux/systemd/rootless Podman 环境执行的宿主证据；只阻塞明确依赖该环境的发布门。 |
| 节点控制面 | node control plane | 可信的 Kernel、Admin 与 MCP 权限域；代表所有者创建和管理沙箱，但不向应用下放节点凭据。 |
| 部署代理 | deployment agent | 经所有者授权，通过 MCP 将应用包转换为已验证沙箱部署的 AI 编程代理。 |
| 版本桶 | version bucket | 单个不可变应用版本专属的 MinIO 桶（`iweb-app-<sandboxId>`）；承载该版本的 celld deployment 对象与运行时状态，其他版本、工作区与系统桶对该桶凭据一律拒绝（真实节点实证）。 |
| 部署对象 | deployment object | 由固定平台命令 `celld deploy` 从已校验快照发布到版本桶的不可变 Worker 产物（`deploy/<script>/<version>/…` + 指针）；对沙箱运行时只读。 |
| 快照物化 | snapshot materialization | supervisor 在创建任何 OCI 资源之前，把内容寻址快照还原到应用容器将只读挂载的目录并按单一摘要权威重算校验；缺失、残缺或摘要不符即拒绝创建沙箱。 |
| 单写队列 | single-writer queue | Kernel 中所有生命周期变更（admit/prepare/readiness/activation/rollback/delete/reconcile/recovery）串行经过的唯一写入通道；并发调用不会产生丢失更新、重复 sequence 或分裂的 active 指针。 |
| 全量中断恢复 | total-outage recovery | 所有沙箱停止且 supervisor 不可达时，Kernel 仅凭持久控制库即可重建控制面；supervisor 恢复后 reconcile 报告缺失并按同一 fail-closed 门恢复 active 沙箱。 |
| 签发父账户 | issuer parent | 预置的 MinIO 用户 `iweb-sandbox-issuer`；每个版本/数据凭据是它下面的服务账户，由 mc 自生成密钥（argv 零凭据），内联策略收窄到单桶，retire 按密钥移除。 |
| 控制/恢复面 | control/recovery plane | 永久归 Kernel 所有的 `api.<base>`；应用或 Admin 损坏时仍可独立访问。 |
| 应用分发器 | Dispatcher | 当前镜像内置 handler 的过渡性单一 Worker 部署入口；不是开放应用的永久沙箱架构。 |
| 原生静态资产 | native static assets | celld 从部署 bucket 提供的不可变文件集合，不是 Dispatcher JavaScript 内嵌数据。 |
| 资产 binding | asset binding | Worker 环境中的 `ADMIN_ASSETS`，通过 `fetch` 读取已部署 Admin 资源。 |
| 资产权威 | asset authority | 浏览器资源字节的唯一真实来源；当前仅为 celld 原生资产集合。 |
| 部署输入目录 | deployment input directory | 镜像内 `/opt/iweb/worker/admin-assets`；仅用于 `celld deploy`，不是第二份运行时资产源。 |
| 路径别名 | path alias | `<base>/<app>/app`，保持同一应用身份并为相对资源提供应用 base path。 |
| owner key | owner key | `IWEB_API_TOKEN` 提供的 bootstrap 所有者凭据；浏览器仅在当前标签页保存。 |
| 进程生命周期指标 | process-lifecycle metrics | Kernel 当前进程内存中的状态投影，不代表持久历史监控。 |
| 应用指标 | application metrics | Kernel 可按路由应用归因的请求、错误、进行中请求与代理延迟；不包含伪精确的应用内存。 |
| celld fleet 应用 | celld fleet application | 一个 fleet 的单一已提交部署；当前 Dispatcher 聚合多个 handler 的形态不满足开放应用的沙箱边界。 |
| 工作区应用 | workspace application | 工作区中的产品级应用身份；必须映射到独立应用沙箱后才能执行，不能再等同于 Dispatcher 分支。 |
| 容器内存包络 | container memory envelope | cgroup `memory.current` 总账；包含 MinIO、celld、Kernel、Caddy、内核记账与文件缓存，不是 celld Worker heap。 |
| resident cell | resident cell | 当前驻留内存的 Durable Object；不是工作区应用，也不等于一个独立 celld 进程。官网 `0.47 MB` 是 trivial cell 基准增量，不是应用启动内存。 |
| 静默节点备份 | quiesced node backup | 先停止当前节点，再从持久卷采集 MinIO、celld、Kernel 的同一停机时点状态；仅对活跃目录做 staging 复制不构成跨服务一致快照。 |

```text
admin-console/build
       | image copy
       v
worker/admin-assets -- celld deploy --> fleet asset store
                                           |
Dispatcher -- selects admin --> Admin handler -- ADMIN_ASSETS.fetch()
```
